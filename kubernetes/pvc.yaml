apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: flask-logs-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: standard  # minikube default storage class
