apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
  labels:
    app: flask-app
spec:
  type: NodePort
  selector:
    app: flask-app
  ports:
  - port: 5001
    targetPort: 5001
    nodePort: 30001  # Access via minikube IP:30001
    protocol: TCP
    name: http
